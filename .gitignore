data/.DS_Store
# only ignore tripdata files but keep weather around
# as that has no clean download link but requires a kaggle account
*tripdata.csv
data/final/weather_trips_combined.parquet
data/final/weather_hourly_all_locations.parquet # too big
data/checkpoints
.DS_Store
.vscode/*
.cache.sqlite